# Native version map

| Wrapper | iOS SDK | Android SDK |
| --- | --- | --- |
| `nuxie_flutter` 0.1.0 | exact `0.1.0` | exact `ai.nuxie:nuxie-android:0.1.0` |

The package manifests are authoritative. Floating branches and local project
fallbacks are unsupported release inputs.
