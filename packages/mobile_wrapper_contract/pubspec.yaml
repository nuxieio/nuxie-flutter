name: mobile_wrapper_contract
description: Shared contract models and fixtures for Nuxie mobile wrappers.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
