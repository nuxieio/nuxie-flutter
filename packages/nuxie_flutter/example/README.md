# Nuxie Flutter example

The example configures the development environment, captures an event, checks
a Feature with remote policy, observes typed native activity and App Actions,
and installs a small app-owned purchase controller.

```bash
flutter run --dart-define=NUXIE_API_KEY=NX_DEV_...
```
